<h1>posts</h1>

<table>
  <tr>
    <th>Account</th>
    <th>Group</th>
    <th>Consultant</th>
    <th>Subject</th>
    <th>Level</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.account.name %></td>
    <td><%= post.group.name %></td>
    <td><%= post.consultant.name %></td>
    <td><%= post.subject.name %></td>
    <td><%= post.rating.name %></td>
    <td><%= link_to 'Show', post %></td>
  </tr>
<% end %>
</table>

<br>
<%= link_to 'New Post', new_post_path %>

