<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @group.name %>
</p>

<p>
  <b>account:</b>
  <%= @group.account.name %>
</p>

<p>
	<b>subjects:</b>
</p>

<table>
  <tr>
		<th>Name</th>
		<th>Account</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @group.subjects.each do |subject| %>
  <tr>
	  <td><%= subject.name %></td>
    <td><%= subject.account.name %></td>
    <td><%= link_to 'Show', subject %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to 'Edit', edit_group_path(@group) %> 